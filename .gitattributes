# Normalize all text files to LF in the repository and on checkout,
# so stored blobs stay LF and checkouts are deterministic on every OS.
* text=auto eol=lf

# Binary assets — never normalize line endings or treat as text.
*.woff2 binary
